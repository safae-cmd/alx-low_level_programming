#include "main.h"
#include <stdio.h>

/**
 * main - prints number of command line arguments
 * @argc: count
 * @argv: string
 * Return: 0
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
